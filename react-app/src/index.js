import React from 'react';
import ReactDOM from 'react-dom';
import App from "./components/App";

// lower case refers to html element
// Pascal case refers to React component (by convention)

ReactDOM.render(
  <App />,
  document.getElementById('root')
);